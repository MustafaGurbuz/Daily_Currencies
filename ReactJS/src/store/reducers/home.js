const initialize = {
    error: false,
    building: false
};


const reducer = (state = initialize, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;

